import google.generativeai as genai

# -----------------------------------------------------------
# 🔑 ใส่ API Key ของนายตรงนี้ (เอาให้ชัวร์ว่าก๊อปมาครบ)
# -----------------------------------------------------------
GOOGLE_API_KEY = "AIzaSyB9aUexVTEUkHTXlK9MPKve4LMQ5yAmKqw"

genai.configure(api_key=GOOGLE_API_KEY)

print("กำลังตรวจสอบโมเดล... กรุณารอสักครู่ ⏳")

try:
    print("-" * 30)
    # ดึงรายชื่อโมเดลทั้งหมดที่ Key นี้มีสิทธิ์ใช้
    for m in genai.list_models():
        # กรองเอาเฉพาะตัวที่คุยโต้ตอบได้ (generateContent)
        if 'generateContent' in m.supported_generation_methods:
            print(f"✅ ใช้ตัวนี้ได้: {m.name}")
    print("-" * 30)
    print("เลือกมา 1 ชื่อ แล้วเอาไปใส่ในไฟล์ llm_explainer.py เลย!")

except Exception as e:
    print(f"❌ เกิดข้อผิดพลาด: {e}")
    print("ถ้าขึ้นแบบนี้ แสดงว่า API Key อาจจะผิด หรือเน็ตมีปัญหา")