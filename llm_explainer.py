import requests
import json

# ----------------------------------------------------
# ⚙️ Configuration
# ----------------------------------------------------
# URL ของ Ollama (ต้องมั่นใจว่ารัน ollama serve อยู่)
OLLAMA_URL = "http://localhost:11434/api/generate"

# รุ่นโมเดลที่จะใช้ (แนะนำ qwen2.5:7b-instruct หรือ typhoon-v1.5-8b-instruct)
MODEL = "qwen2.5:7b-instruct"

# ----------------------------------------------------
# 🔧 Internal Helper Function (ฟังก์ชันช่วยภายใน)
# ----------------------------------------------------
def _call_llm(prompt: str) -> str:
    """
    ฟังก์ชันกลางสำหรับยิง Request ไปหา Ollama
    - มี try-except กันแอปพังถ้าไม่ได้เปิด Ollama
    - ตั้ง Timeout ไว้ที่ 90 วินาที (เผื่อเครื่องช้า)
    """
    try:
        # ยิง Request ไปที่ Localhost
        res = requests.post(
            OLLAMA_URL,
            json={
                "model": MODEL,
                "prompt": prompt,
                "stream": False,       # ปิด Stream เพื่อรอข้อความเต็มทีเดียว
                "options": {
                    "temperature": 0.3, # ค่าต่ำ = AI ไม่มั่ว (Creative น้อยลง เน้น Fact)
                    "top_p": 0.9
                }
            },
            timeout=90  # รอสูงสุด 90 วินาที
        )
        res.raise_for_status() # เช็คว่า HTTP Status OK ไหม (200)
        return res.json().get("response", "").strip()

    except requests.exceptions.ConnectionError:
        return "⚠️ ไม่สามารถเชื่อมต่อกับ AI ได้ (กรุณาตรวจสอบว่าเปิด Ollama หรือยัง?)"
    except requests.exceptions.Timeout:
        return "⚠️ AI ใช้เวลาประมวลผลนานเกินไป (Timeout)"
    except Exception as e:
        return f"⚠️ เกิดข้อผิดพลาด: {str(e)}"

# ----------------------------------------------------
# 🧠 Main Functions
# ----------------------------------------------------

def explain_with_llm(text: str, found_words: list[str]) -> str:
    """
    หน้าที่: อธิบายเหตุผลทางกฎหมาย
    ---------------------------
    รับ: ข้อความเต็ม + คำผิดที่ Rule-based เจอ
    คืนค่า: คำอธิบายสั้นๆ ว่าทำไมคำพวกนี้ถึงผิดกฎ อย.
    """
    # ถ้าไม่มีคำผิดเลย ไม่ต้องเรียก AI ให้เปลืองทรัพยากร
    if not found_words:
        return "ไม่พบคำโฆษณาที่ผิดกฎชัดเจนในรายการตรวจสอบเบื้องต้น"

    prompt = f"""
[บทบาท]
คุณคือ "ผู้เชี่ยวชาญด้านกฎหมายโฆษณาของไทย (อย. และ สคบ.)"
หน้าที่ของคุณคืออธิบายความเสี่ยงของข้อความโฆษณาให้ผู้ประกอบการเข้าใจง่าย

[ข้อมูลที่ตรวจพบ]
ข้อความ: "{text}"
คำที่ระบบ Rule-based จับได้: {', '.join(found_words)}

[คำสั่ง]
กรุณาอธิบายว่าทำไมคำเหล่านี้ถึงมีความเสี่ยง โดยอ้างอิงหลักการ:
1. ห้ามการันตีผลลัพธ์ (เช่น หายขาด, 100%)
2. ห้ามอวดอ้างสรรพคุณเกินจริง
3. ห้ามใช้คำทางการแพทย์กับอาหารเสริม/เครื่องสำอาง

[รูปแบบคำตอบที่ต้องการ]
สรุปความเสี่ยง: (สั้นๆ ไม่เกิน 2 บรรทัด)
เหตุผลทางกฎหมาย:
- (เหตุผลข้อ 1)
- (เหตุผลข้อ 2)
ข้อแนะนำ: (สั้นๆ)

*ตอบเป็นภาษาไทยทางการ ห้ามเกริ่นนำว่า "ได้ครับ" ให้ตอบเนื้อหาเลย*
"""
    return _call_llm(prompt)


def suggest_safe_text(original_text: str) -> str:
    """
    หน้าที่: ร่างข้อความใหม่ (Magic Rewrite)
    ---------------------------
    รับ: ข้อความโฆษณาเดิมที่เสี่ยง
    คืนค่า: ข้อความใหม่ที่ Safe และถูกกฎหมาย แต่ยังขายของได้
    """
    prompt = f"""
[บทบาท]
คุณคือ "Copywriter มืออาชีพ" ที่เชี่ยวชาญกฎหมายโฆษณาไทย

[ภารกิจ]
ปรับแก้ข้อความโฆษณาด้านล่างให้ "ปลอดภัย" และ "ถูกกฎหมาย" 100%
โดยยังคงความน่าสนใจในการขายสินค้า

[ข้อความเดิม]
"{original_text}"

[กฎเหล็กในการแก้]
1. ตัดคำว่า "หายขาด", "100%", "การันตี", "รักษา" ออกให้หมด
2. เปลี่ยนเป็นคำเลี่ยง เช่น "มีส่วนช่วย", "ดูแล", "ผลลัพธ์ขึ้นอยู่กับบุคคล"
3. ห้ามเปลี่ยนความหมายสินค้า (เช่น ถ้าเป็นครีม อย่าแก้เป็นยากิน)
4. ความยาวต้องใกล้เคียงของเดิม

[รูปแบบคำตอบ]
ขอเฉพาะ "ข้อความใหม่" เท่านั้น ห้ามมีคำอธิบายประกอบ ห้ามใส่เครื่องหมายคำพูดเปิดปิด
"""
    return _call_llm(prompt)


def rewrite_sentence_safe(sentence: str) -> str:
    """
    หน้าที่: แก้ไขเฉพาะประโยคเดียว (ใช้สำหรับฟีเจอร์ย่อยถ้ามี)
    """
    prompt = f"""
[คำสั่ง]
แก้ไขประโยคนี้ให้ถูกต้องตามหลักโฆษณา อย. (ห้าม Overclaim):
"{sentence}"

[วิธีแก้]
- เปลี่ยน "รักษา" เป็น "บำรุง/ดูแล"
- เปลี่ยน "หายขาด" เป็น "ดีขึ้น"
- เติม "อาจ" หรือ "มีส่วนช่วย"

ตอบเฉพาะประโยคใหม่เท่านั้น:
"""
    return _call_llm(prompt)